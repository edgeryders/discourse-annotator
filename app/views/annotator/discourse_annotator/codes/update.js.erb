$("#code-row-<%= @requested_resource.id %>").replaceWith('<%= j render("translate_row", resource: @requested_resource, status: @requested_resource.errors.empty? ? 'saved' : 'error') %>');


//setTimeout(function(){
//    $(".saved-notification").fadeOut("slow");
//},1000)

